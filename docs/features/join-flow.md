# Join-flow packets

Unaudited. `FieldAddUser`, `AddPortal`, and the battle-join packet set have
not yet audited at the byte level. The rest of the field-load flow is
aligned (`aligned.md`), but the join sequence itself still needs a byte-level
audit.